# Terraform Provider Neuwerk

This repository publishes Terraform provider release artifacts for source address:

- `moolen/neuwerk`

Development happens in the Neuwerk `firewall` monorepo.

If you want to change provider behavior, schema, tests, docs, or examples, send the pull request to
that monorepo first. Changes in this repository should stay limited to release plumbing required for
public provider publication.
